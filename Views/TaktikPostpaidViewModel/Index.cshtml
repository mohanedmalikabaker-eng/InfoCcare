@model InfoCcare.Models.TaktikPostpaidOfferViewModel
@{
    Layout = "_GradientLayout";
}

@section Styles {
    <link rel="stylesheet" href="~/css/table.css" asp-append-version="true" />
}

<div class="custom-table-container">
    <div class="align-items-md-center">
        <h4 style="text-align:center">"" عروض باقات تك تك – TakTik Offers""</h4>
    </div>
    <table class="custom-table" dir="rtl">
        <thead>
            <tr>
                <th>الباقة</th>
                <th>الوحدات</th>
                <th>السعر (غير شامل الضريبة)</th>
                <th>الرمز</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.Offers)
            {
                <tr>
                    <td>@item.Bundles</td>
                    <td>@item.Units</td>
                    <td>@item.PriceVatExluded.ToString("N0")</td>
                    <td>@item.Code</td>
                </tr>
            }
        </tbody>
    </table>
</div>
    <!-- قسم الوصف -->

    <div dir="rtl" class="custom-table-container">
        <h4 style="text-align:right">الخصائص</h4>
        @foreach (var desc in Model.Descriptions)
        {
            <div class="feature-box">
                <h5>@desc.Title</h5>
                <p class="mb-0">@Html.Raw(desc.Details)</p>
            </div>
        }
    </div>

